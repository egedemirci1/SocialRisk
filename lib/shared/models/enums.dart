enum GameMode {
  classic,
  toxic,   // Gelecek planı
  custom   // Gelecek planı
}

enum GameStatus {
  waiting,
  playing,
  finished
}

enum VoteValue {
  like,
  neutral,
  dislike
}

enum EndConditionType {
  score,
  rounds
}

import 'package:flutter/material.dart';

class AppColors {
  // Arka plan
  static const background       = Color(0xFF0D0D1A);
  static const surface          = Color(0xFF1A1A2E);
  static const surfaceElevated  = Color(0xFF16213E);

  // Vurgu
  static const primary          = Color(0xFFE94560);
  static const secondary        = Color(0xFF0F3460);
  static const accent           = Color(0xFFFFD700);

  // Oyun durumu renkleri
  static const fire             = Color(0xFFFF4500);
  static const ice              = Color(0xFF00BFFF);
  static const glow             = Color(0xFF9D4EDD);

  // Oylama renkleri
  static const votePositive     = Color(0xFF4CAF50);
  static const voteNeutral      = Color(0xFFFF9800);
  static const voteNegative     = Color(0xFFF44336);

  // Ceza / Hata
  static const error            = Color(0xFFE53935);
  static const penalty          = Color(0xFFE53935);
  static const passWarning      = Color(0xFFFFC107);
}

import 'package:flutter_test/flutter_test.dart';

void main() {
  test(
    'VotingPanel widget tests skipped — timer-sensitive; re-enable after harness',
    () {},
    skip: 'Pending VotingPanel timer refactor',
  );
}
